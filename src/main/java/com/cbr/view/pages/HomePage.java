package com.cbr.view.pages;

import javafx.scene.control.Label;
import javafx.scene.layout.StackPane;

public class HomePage extends StackPane {
    public HomePage() {
        super();
        this.getChildren().add(new Label("Homepage"));
    }
}
